import React, { useRef, useState } from "react";
import { IoIosAddCircleOutline } from "react-icons/io";
import { preProcessFile } from "typescript";

const PostUpload = () => {
  const [memberFile, setMemberFile] = useState([]);

  return <></>;
};

export default PostUpload;
